{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch GMS in Chrome",
            "url": "http://localhost/gg_management/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}